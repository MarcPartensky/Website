version: "3.9"
services:
  web:
    env_file: .env
    build: .
    ports:
      - 80:8000
      - 21:10021
    # volumes:
    #   - .:.
    links:
      - redis
    # networks:

  redis:
    image: redis:alpine
